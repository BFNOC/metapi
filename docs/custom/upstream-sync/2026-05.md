# 上游同步记录 — 2026 年 5 月

> 归档自 [upstream-sync-log.md](../upstream-sync-log.md)，涵盖 2026-05-10 的审阅与实施。

---

## 2026-05-10 审阅（`262651a` → `cdb12a9`）

**上游已审阅到**：`cdb12a9`（`upstream/main` HEAD，2026-05-08 合入）

相对上次基线 `262651a`，上游主线新增 27 个 commit（含 merge）。

### 分诊结论

| PR / Commit | 说明 | 判断 | 理由 |
|-------------|------|------|------|
| #495 (3 commits) | auto-discover recent Docker Hub dev tags in Update Center | ❌ 跳过 | K3s / Update Center 长期跳过策略 |
| #510 (4 commits) | feat(sites): per-site model probe with streaming logs | ✅ 已合入 | 站点级模型探测，实用功能 |
| #518 (1 commit) | fix(settings): persist model allowlist as JSON array | ✅ 已合入 | 修复 double JSON encoding bug |
| #533 (6 commits) | Harden cross-protocol request compatibility | ✅ 已合入 | 协议兼容性加固 |
| #535 (1 commit) | fix(ci): clear production dependency audit | ❌ 跳过 | 仅 package-lock.json 变更 |
| #505 (1 commit) | feat: 支持从站点页快捷添加 API Key | ✅ 已合入 | Sites.tsx 快捷添加 Key 按钮 |
| #490 (1 commit) | [codex] fix quota refresh probe model | ❌ 跳过 | OAuth 线长期跳过策略（本地无 probe 功能） |
| (2 commits) | README contributor section cleanup | ❌ 跳过 | 文档类 |

---

## 2026-05-10 实施（#518）

| 说明 | 合入方式 |
|------|---------|
| fix: persist model allowlist as JSON array | 手工移植 |

**repo-local 差异要点**：
- 本地无 `global_allowed_models` 功能和独立 `runtimeSettingsHydration.ts`
- 仅修复 `global_blocked_brands` 的 `JSON.stringify` 双重编码
- 在 `index.ts` hydration 中增加对历史坏数据的 JSON.parse fallback

**验证**：typecheck 通过

---

## 2026-05-10 实施（#510）

| 说明 | 合入方式 |
|------|---------|
| feat(sites): per-site model probe with streaming logs and auto-disable | 手工移植 |

**repo-local 差异要点**：
- 引入架构拆分最小子集：`orchestration/endpointFlow.ts` + `orchestration/upstreamRequest.ts`（新文件）
- 创建 `runtimeDispatch.ts` / `upstreamEndpointRuntime.ts` 薄壳 re-export（指向本地已有文件）
- 创建 `runtimeModelProbe.ts`（250 行，走真实代理路径探测）
- 新增 `probeSiteModels()` 服务函数（并发 worker-pool、延迟阈值、进度回调）
- 新增 `/api/sites/:id/probe-now`（JSON）和 `/api/sites/:id/probe-stream`（SSE）端点
- 前端站点编辑弹窗新增探测配置面板 + 实时日志
- 保留本地已有的 `/api/sites/:id/probe-models` 端点不变（两套共存）
- config 新增 `modelAvailabilityProbeTimeoutMs`

**验证**：server + web typecheck 通过

---

## 2026-05-10 实施（#505）

| 说明 | 合入方式 |
|------|---------|
| feat: 支持从站点页快捷添加 API Key | 手工移植 |

**repo-local 差异要点**：
- 重构 `handleSiteCreatedChoice` 为 `openSiteConnectionFlow()` 统一入口
- 桌面端和移动端站点列表新增"添加 Key"按钮
- 不跟上游的 `initializationPresetId` 参数（本地无此功能）

**验证**：web typecheck 通过

---

## 2026-05-10 实施（#533）

| 说明 | 合入方式 |
|------|---------|
| Harden cross-protocol request compatibility | 手工移植 + git apply |

**repo-local 差异要点**：
- 新文件直接从 upstream 提取：`responsesPreflight.ts`、`serviceTierPolicy.ts`、`webSearchSimulation.ts` + tests
- transformer 改动通过 `git apply --3way` 无冲突应用
- surfaces 手工添加 import + 调用点（preflight、web search simulation）
- 未移植 service tier policy 在 chatSurface 的完整集成（仅添加 import，实际调用需要 endpoint 层重构后的 `bodyForEndpoint` 模式）
- config 新增 `openAiServiceTierRules` 环境变量

**验证**：server typecheck 通过
