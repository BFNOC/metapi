# 上游同步记录 — 2026 年 4 月后半段

> 归档自 [upstream-sync-log.md](../upstream-sync-log.md)，涵盖 2026-04-06 至 2026-04-11 的审阅与实施。

---

## 2026-04-06 审阅（v1.3.0）

**上游已审阅到**：`63c435c`（`v1.3.0` tag，2026-04-06T07:55:55Z）

相对上次基线 `63f6b07`，上游主线新增 26 个 commit。

### 已落地

| Commit | PR | 说明 | 合入方式 | 本地提交 |
|--------|-----|------|---------|---------|
| `b3e987a` | #416 | codex continuation scope roundtrips | 无需重复合入 | `dc86d9f`（2026-04-05 已先行补齐） |
| `193f3e7` | #428 | proxy log usage source metadata | 无需重复合入 | 本地已等价覆盖 |
| `5b005d8` | #422 | tighten generic upstream passthrough headers | 手工移植 | 见下方实施节 |
| `3e8d69b` | #439 | codex compact non-stream accept header | 手工移植 | 见下方实施节 |
| `df75a80` | #426 | responses compact fallback handling | 手工移植 | `f7b6ad9` |
| `6feda0e` | #429 | downstream client detection boundaries | 手工移植 | 见下方实施节 |
| `a789ddf` | #441 | sub2api managed refresh resilience | 手工移植 | 见下方实施节 |
| `42f9049` | #444 | antigravity special-model non-stream path | 手工移植 | 见下方实施节 |

<details>
<summary>v1.3.0 筛选说明</summary>

**repo-local 锚点**：
- `upstreamEndpoint.ts`：`extractSafePassthroughHeaders()` 已从黑名单改为 allowlist（#422）
- `headerUtils.ts`：Codex runtime header 已支持 `stream` 参数选择 `Accept` 头（#439）
- `openAiResponsesSurface.ts`：compact 请求已有专用 fallback matcher 和运行时开关（#426）

**落地方式**：不直接 cherry-pick，继续沿用 hand-port 模式，保持本地语义与调用链不被上游新体系反向牵动。

</details>

---

## 2026-04-06 实施（#422 / #439）

| Commit | 说明 | 合入方式 |
|--------|------|---------|
| `5b005d8` | tighten generic upstream passthrough headers (#422) | 手工移植 |
| `3e8d69b` | codex compact non-stream accept header (#439) | 手工移植 |

**repo-local 差异要点**：
- generic passthrough 从黑名单式过滤改为 allowlist；Codex 专用 passthrough 仅保留 `version` / `x-responsesapi-include-timing-metrics`
- `buildCodexRuntimeHeaders()` 新增 `stream` 参数，非流式返回 `Accept: application/json`
- `x-metapi-*` 头仅参与内部逻辑（websocket transport 默认值判定），不作为上游透传头发出

**验证**：targeted tests 100 passed · drift-check 0 · typecheck passed

---

## 2026-04-06 实施（#426）

| Commit | 说明 | 合入方式 | 本地提交 |
|--------|------|---------|---------|
| `df75a80` | Fix responses compact fallback handling (#426) | 手工移植 | `f7b6ad9` |

**repo-local 差异要点**：
- compact fallback 只允许 `/responses/compact → /responses`，不允许 compact → chat / messages
- 404 fallback matcher 额外收紧：仅 compact hint 的 404 才触发，避免"模型不存在"类 404 误判
- 新增运行时开关 `responsesCompactFallbackToResponsesEnabled`（Settings UI + 持久化）

**验证**：targeted tests 25 passed · drift-check 0

---

## 2026-04-06 实施（#429）

| Commit | 说明 | 合入方式 |
|--------|------|---------|
| `6feda0e` | fix downstream client detection boundaries (#429) | 手工移植 |

**repo-local 差异要点**：
- Detection 边界完全跟随 upstream：Codex 不再识别 `/v1/messages`，`/v1/responses` 收紧为精确/前缀匹配
- 新增 Claude Code header fallback（`user-agent=claude-cli/...` + `anthropic-*` headers）
- 新增 OpenCode app fingerprint（`x-title` / `user-agent` + system prompt heuristic）
- 不跟上游的 owner 迁移（`downstreamClientContext.ts` 保留在 routes/ 下）

**验证**：targeted tests 38 passed · drift-check 0

---

## 2026-04-06 实施（#441）

| Commit | 说明 | 合入方式 |
|--------|------|---------|
| `a789ddf` | add sub2api managed refresh resilience (#441) | 手工移植 |

**repo-local 差异要点**：
- 三层落地：shared helper（`isSub2ApiPlatform` / `refreshSub2ApiManagedSession` + failure detail）→ singleflight（按 `account.id` 粒度）→ scheduler（1min 周期、bounded concurrency 4）
- helper 使用本地既有 proxy helpers，scheduler 挂在 `index.ts` 独立后台服务区
- 未扩到 settings 开关、Web UI、schema、其他 managed-token 平台

**验证**：targeted tests 16 passed · build passed · drift-check 0

---

## 2026-04-06 实施（#444）

| Commit | 说明 | 合入方式 |
|--------|------|---------|
| `42f9049` | align antigravity special-model non-stream path with CPA (#444) | 手工移植 |

**repo-local 差异要点**：
- Shared helper `shouldUseAntigravityStreamAction()` / `resolveAntigravityProviderAction()`，special-model 范围：Claude family + `gemini-3-pro*` + `gemini-3.1-flash-image`
- Provider profile / gemini surface 对齐：special-model 非流式也走 internal stream endpoint
- Executor 聚合闭环：upstream SSE → downstream JSON，含 tail / fallback aggregation
- 不扩到 generic Gemini / gemini-cli 重构

**验证**：targeted tests 111 passed · build passed · drift-check 0

---

## 2026-04-08 实施（`569a15e` / Accounts 子线）

| Commit | 说明 | 合入方式 |
|--------|------|---------|
| `569a15e` (子集) | 批量 API Key 连接创建 + Accounts 页批量输入交互 | 手工移植 |

**repo-local 差异要点**：
- 只摘 batch API key create / UI 交互，不跟 tokenRouter / workflow / dependency bump
- 后端抽出 `createManualAccount()`，支持 `accessTokens: string[]` 批量创建，返回 batch 聚合结果
- 前端 API Key 分段新增批量输入识别，批量模式不要求预验证
- 保留既有 owner（`skipModelFetch`、Session/APIKey 分段、Sub2API refreshToken），未引入上游新文件布局

**验证**：targeted tests 22 passed · drift-check 0

---

## 2026-04-08 实施（`569a15e` / tokenRouter 子线）

| Commit | 说明 | 合入方式 |
|--------|------|---------|
| `569a15e` (子集) | 冲突时优先命中 explicit-group display-name alias | 手工移植 |

**repo-local 差异要点**：
- `findRoute()` 匹配顺序改为：explicit_group + displayName → colliding exact → 普通 displayName / pattern
- 当前线上无启用中的 `displayName` / `explicit_group` 路由（均为 0），语义切换暂未立即生效
- 只改 3 文件：`tokenRouter.ts` + 两个测试文件

**验证**：targeted tests 37 passed · drift-check 0

---

## 2026-04-11 审阅（post-v1.3.0，`569a15e` → `a2c2ae6`）

**上游已审阅到**：`a2c2ae6`（`upstream/main` HEAD，2026-04-10）

相对上次基线 `569a15e`，上游主线新增 9 个 commit（不含 merge）。

### 已落地

| Commit | PR | 说明 | 合入方式 | 本地提交 |
|--------|-----|------|---------|---------|
| `c6a28c7` | #464 | improve LobeHub brand detection coverage | `git cherry-pick` | `4b28357` |
| `9c766de` + `a2c2ae6` | #473 + #474 | payload rules settings UI + upstream types | 手工移植 | 见下方实施节 |

<details>
<summary>#464 分析</summary>

7 文件 +1163/-585 行。本地 `brandMatcher.ts`、`brandRegistry.ts` 等文件经 `git hash-object` 验证与上游 byte-identical。改动含：品牌匹配逻辑抽取到 `src/server/shared/modelBrand.ts`、新增 `getMatchingBrandNames()` 多命中支持、blocked brand 大小写宽容、新增 ~30 个品牌。三个 commit 是完整语义闭环，零冲突整包 cherry-pick。

</details>

<details>
<summary>#473 + #474 分析</summary>

**#473**：8 文件 +1673 行。后端引擎 `payloadRules.ts` 已完整，但缺 API 端点和前端 UI（当前只能改环境变量重启）。上游新增 `parsePayloadRulesConfigInput()` 验证函数（纯追加）+ Settings API 接入 + 可视化规则编辑器 + 高级 JSON 编辑器。

**#474**：3 文件 +46/-11 行。协议选项列表扩展支持 `sub2api`/`new-api`/`cliproxyapi` 等上游类型。移植时直接采用 #474 最终版，避免先合再改。

</details>

### 附带修复

| 本地提交 | 说明 |
|----------|------|
| `b735042` | fix: add missing `proxyToken` to `RuntimeSettingsPayload` type |

---

## 2026-04-11 实施（#464）

| Commit | PR | 说明 | 合入方式 | 本地提交 |
|--------|-----|------|---------|---------|
| `c6a28c7` | #464 | improve LobeHub brand detection coverage | `git cherry-pick` | `4b28357` |

**验证**：品牌相关 4 suites 24 passed · drift-check 0 · typecheck 全部通过

---

## 2026-04-11 实施（#473 + #474）

| Commit | PR | 说明 | 合入方式 |
|--------|-----|------|---------|
| `9c766de` | #473 | add payload rules settings UI | 手工移植 |
| `a2c2ae6` | #474 | expose more payload-rule upstream types | 手工移植 |

**repo-local 差异要点**：
- 后端：纯追加 `parsePayloadRulesConfigInput()`（+194 行），Settings API `GET/PUT` 接入，采用 deferred-write 防误写
- 前端：可视化规则编辑器（动作/模型/协议/参数值）+ Codex 高推理预设一键添加 + 高级 JSON 编辑器（5 textarea）+ 双向同步 + 脏状态确认
- 直接采用 #474 最终版：15 个上游平台协议选项
- 保留本地 settings.ts owner 和 validate-then-persist 流程

**验证**：targeted tests 42 passed · drift-check 0 · typecheck 全部通过
